[project]
name = "eco-planner"
version = "0.1.0"
description = "Energy-aware diffusion planning in MetaDrive with DPPO fine-tuning."
readme = "README.md"
requires-python = ">=3.10,<3.11"
dependencies = [
    "fastsim>=3.0",
    "gymnasium>=1.0",
    "hydra-core>=1.3",
    "lightning>=2.5",
    "metadrive-simulator",
    "numpy>=1.26",
    "omegaconf>=2.3",
    "timm==1.0.10",
    "torch>=2.5",
]

[dependency-groups]
dev = [
    "pytest>=8.3,<9",
    "ruff>=0.9,<1",
]

[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"

[tool.uv.sources]
metadrive-simulator = { path = "third_party/metadrive", editable = true }

[tool.hatch.build.targets.wheel]
packages = ["src/eco_planner"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra --import-mode=importlib"
markers = [
    "gpu: requires a CUDA GPU",
    "simulator: requires MetaDrive and its simulator runtime",
    "slow: long-running integration or evaluation test",
]

[tool.ruff]
target-version = "py310"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
